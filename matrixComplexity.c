#include <stdio.h>
#include <stdlib.h>

#define N 10
#define M N*N

int matrixMultiplication(int dim, int A[dim][dim], int B[dim][dim]);
void matrixReading(int C[M][M]);

int main(){
    int dim;
    printf("matrix complexity: ");
    return 0;
}

int matrixMultiplication(int dim, int A[dim][dim], int B[dim][dim]){
    int i, j;
    for(i = 0; i < dim; i++){
        for(j = 0; j < dim; j++){
            
        }
    }
};